version: "3.9"

services:
  stuttgart-gophers:
    build:
      context: ./
      dockerfile: Dockerfile
    restart: always

  caddy:
    image: caddy
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
    